export interface CategriesData {
    id: number;
    name: string;
    slug: string;
    createdAt: string;
    updatedAt: string;
}

export interface Categry {
  id: number;
  name: string;
  slug: string;
  createdAt: string;
  updatedAt: string;
}